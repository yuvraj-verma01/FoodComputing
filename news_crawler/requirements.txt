# ============================================================
# Food Oil News Crawler — Python Dependencies
# Python 3.10+
# ============================================================

# --- Core HTTP ---
requests>=2.31.0
urllib3>=2.0.0

# --- HTML Parsing & Article Extraction ---
trafilatura>=1.12.0           # primary extractor (best boilerplate removal)
beautifulsoup4>=4.12.0        # fallback extractor + metadata
lxml>=5.0.0                   # fast HTML parser for bs4

# --- newspaper (try newspaper4k first, fall back to newspaper3k) ---
# Install ONE of these (newspaper4k is the maintained fork):
newspaper4k>=0.9.3.1          # actively maintained fork of newspaper3k
# newspaper3k>=0.2.8          # legacy (use if newspaper4k fails to install)

# --- RSS ---
feedparser>=6.0.10

# --- Config / Env ---
pyyaml>=6.0.1
python-dotenv>=1.0.0

# --- CLI ---
click>=8.1.7
tqdm>=4.66.0

# --- Storage ---
# sqlite3 is in stdlib; no extra package needed

# --- Date Parsing ---
python-dateutil>=2.9.0

# --- Seed loading (docx) ---
python-docx>=1.1.0

# --- Testing ---
pytest>=8.0.0
pytest-cov>=5.0.0

# --- Optional: Near-Duplicate Hashing (install for faster MinHash) ---
# datasketch>=1.6.5           # uncomment if processing >10k articles

# --- Optional: Zero-shot classification (LLM integration stub) ---
# transformers>=4.40.0        # for HuggingFace zero-shot pipeline
# torch>=2.0.0                # required by transformers
# anthropic>=0.27.0           # for Claude API classification

# ============================================================
# Model Training Module  (src/model_training/)
# ============================================================

# --- Core ML ---
scikit-learn>=1.4.0
numpy>=1.26.0
pandas>=2.2.0
joblib>=1.3.0

# --- Sentence Transformers (Model 4: SBERT embeddings) ---
sentence-transformers>=2.7.0
# pulls in: torch, transformers, huggingface-hub

# --- Excel output (for review workbooks already in use) ---
openpyxl>=3.1.0

# --- Optional: HuggingFace fine-tuning (Model 5, --include-hf flag) ---
# These are heavy; install only if you plan to run --include-hf
# torch>=2.0.0
# transformers>=4.40.0
# accelerate>=0.29.0
