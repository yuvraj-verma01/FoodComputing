"use client";

import { ChevronRight, GitBranch } from "lucide-react";
import Link from "next/link";
import { useMemo, useState } from "react";
import type { ArticleSummary, TaxonomyData } from "@/lib/types";
import { cn, formatNumber } from "@/lib/utils";

export function TaxonomyTree({ taxonomy, articles }: { taxonomy: TaxonomyData; articles: ArticleSummary[] }) {
  const [mode, setMode] = useState<"food" | "issue">("food");
  const nodes = mode === "food" ? taxonomy.food_categories : taxonomy.issue_categories;
  const [selectedId, setSelectedId] = useState(nodes[0]?.id ?? "");
  const selected = nodes.find((node) => node.id === selectedId) ?? nodes[0];
  const related = useMemo(() => mode === "food" && selected ? articles.filter((article) => article.food_keyword === selected.name && article.human_label === "relevant") : [], [articles, mode, selected]);

  function changeMode(value: "food" | "issue") { setMode(value); const next = value === "food" ? taxonomy.food_categories : taxonomy.issue_categories; setSelectedId(next[0]?.id ?? ""); }
  if (!selected) return null;
  return <div><div className="inline-flex border border-[var(--line)] bg-white p-1" role="tablist" aria-label="Taxonomy type"><button role="tab" aria-selected={mode === "food"} className={cn("focus-ring min-h-10 px-4 text-sm font-semibold", mode === "food" && "bg-[var(--maroon)] text-white")} onClick={() => changeMode("food")}>Food categories</button><button role="tab" aria-selected={mode === "issue"} className={cn("focus-ring min-h-10 px-4 text-sm font-semibold", mode === "issue" && "bg-[var(--maroon)] text-white")} onClick={() => changeMode("issue")}>Issue categories</button></div><div className="mt-7 grid gap-7 lg:grid-cols-[20rem_minmax(0,1fr)]"><nav className="border border-[var(--line)] bg-[var(--white)]" aria-label={`${mode} taxonomy nodes`}><div className="border-b border-[var(--line)] p-5"><p className="flex items-center gap-2 text-sm font-semibold"><GitBranch className="h-4 w-4 text-[var(--saffron)]" />Provisional hierarchy</p></div>{nodes.map((node) => <button key={node.id} onClick={() => setSelectedId(node.id)} className={cn("focus-ring flex w-full items-center justify-between border-b border-[var(--line)] px-5 py-3.5 text-left text-sm last:border-0 hover:bg-[var(--paper-deep)]", selected.id === node.id && "bg-[var(--maroon)] text-white hover:bg-[var(--maroon)]")}><span>{node.name}</span><ChevronRight className="h-4 w-4" /></button>)}</nav><article className="border border-[var(--line)] bg-[var(--white)]"><div className="border-b border-[var(--line)] p-6 md:p-8"><div className="flex flex-wrap items-center gap-2"><span className="rounded-full border border-amber-800/25 bg-amber-50 px-2.5 py-1 text-xs font-semibold text-amber-900">Provisional interface taxonomy</span><code className="text-xs text-[var(--muted)]">{selected.id}</code></div><h2 className="font-editorial mt-5 text-4xl text-[var(--maroon-dark)]">{selected.name}</h2><p className="mt-4 max-w-3xl leading-7 text-[var(--muted)]">{selected.definition}</p></div><div className="grid gap-px bg-[var(--line)] sm:grid-cols-3"><TaxonomyMetric label="Parent category" value={selected.parent_id ?? "Top level"} /><TaxonomyMetric label="Child categories" value={selected.children?.length ? formatNumber(selected.children.length) : "None supplied"} /><TaxonomyMetric label="Incident count" value={mode === "food" ? formatNumber(related.length) : "Extraction pending"} /></div><div className="grid gap-8 p-6 md:grid-cols-2 md:p-8"><div><h3 className="text-sm font-semibold">Related articles</h3>{related.length ? <ul className="mt-4 grid gap-3 text-sm">{related.slice(0, 5).map((article) => <li key={article.article_id}><Link className="focus-ring text-[var(--maroon)] hover:underline" href={`/incidents/${article.slug}`}>{article.title}</Link></li>)}</ul> : <p className="mt-3 text-sm italic text-[var(--muted)]">No validated ontology-linked articles yet.</p>}</div><div><h3 className="text-sm font-semibold">FSSAI relationship</h3><p className="mt-3 text-sm leading-6 text-[var(--muted)]">{selected.fssai_relationship ?? "Not yet mapped"}</p><h3 className="mt-7 text-sm font-semibold">Ontology identifier</h3><p className="mt-3 text-sm italic text-[var(--muted)]">{taxonomy.status === "validated" ? selected.id : "Validated ontology identifier not yet supplied"}</p></div></div></article></div></div>;
}

function TaxonomyMetric({ label, value }: { label: string; value: string }) { return <div className="bg-[var(--paper-deep)] p-5"><p className="text-xs font-semibold uppercase tracking-[.06em] text-[var(--muted)]">{label}</p><p className="mt-2 text-sm font-semibold">{value}</p></div>; }
